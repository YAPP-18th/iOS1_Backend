<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yapp.ios1.mapper.FollowMapper">
    <select id="getFollowCountByUserId" resultType="int">
        SELECT COUNT(*)
        FROM follow
        WHERE following_id = #{userId};
    </select>

    <select id="getFollowListByUserId" resultType="FriendDto">
        SELECT u.id as userId, u.email, u.intro
        FROM follow f
                 JOIN user u ON f.follower_id = u.id
        WHERE following_id = #{userId};
    </select>

    <select id="isFriendByMyUserIdAndOtherUserId" resultType="int">
        SELECT COUNT(id)
        FROM follow
        WHERE ((following_id = #{myUserId} AND follower_id = #{otherUserId}) OR
               (following_id = #{otherUserId} AND follower_id = #{myUserId}))
          AND friend_status = 2;
    </select>

    <insert id="followRequest">
        INSERT INTO follow (following_id, follower_id, friend_status, alarm_id)
        VALUES (#{friendId}, #{myUserId}, #{followRequest}, #{alarmId})
    </insert>

    <update id="followAccept">
        UPDATE follow
        SET friend_status = #{friendStatus}
        WHERE following_id = #{myUserId} AND follower_id = #{friendId}
    </update>
</mapper>

